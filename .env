ADMIN_PASSWORD=
JWT_SECRET=
PORT=3737
DISCOGENIUS_BIND_IP=0.0.0.0
# Optional: set for non-default API target
# VITE_API_URL=http://localhost:3737
# Log any main-thread DB read blocking the event loop longer than this (ms).
# Diagnostic for UI sluggishness under load; cheap enough to leave on.
DISCOGENIUS_READ_PROFILE_MS=250
